import json, os

contacts = {}
import json, os

contacts = {}
def load_contacts():
    global contacts

if os.path.exists("contacts.json"):
        with open("contacts.json", "r") as f:
	contacts = json.load(f)

def save_contacts():`
with open("contacts.json", "w") as f:
        json.dump(contacts, f, indent=2)
	def add_contact():
    name = input("Name: sjkl ")
    phone = input("Phone: 234567")
    email = input("Email: gf@123email.com")
    contacts[name] = {"Phone": phone, "Email": email}
    save_contacts()
    def view_all():
    for name, info in contacts.items():
    print(f"Name: {name}\nPhone: {info['Phone']}\nEmail: {info['Email']}\n")

def search_contact():
name = input("Enter name: ")
    if name in contacts:
    print("Found:", contacts[name])
    else:
        print("Contact not found.")
	load_contacts()
while True:
print("\n1. Add\n2. View All\n3. Search\n4. Exit")
    choice = input("Choice: ")
    if choice == '1':
        add_contact()
